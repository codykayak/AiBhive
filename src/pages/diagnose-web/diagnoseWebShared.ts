import { Cable, Droplets, Layers, Wind, Wrench, Zap } from 'lucide-react';

export const PACK_ICONS = {
  waves: Droplets,
  zap: Zap,
  wrench: Wrench,
  droplets: Droplets,
  wind: Wind,
  cable: Cable,
} as const;
